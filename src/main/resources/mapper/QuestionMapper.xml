<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.QuestionMapper">

	<select id="list" resultType="com.example.domain.QuestionVO">
		select * from tbl_question order by question_bno desc;
	</select>
	
	 <select id="read" resultType="com.example.domain.QuestionVO">
	 select * from tbl_question where question_bno=#{question_bno};
	 </select>
	 
	 <insert id="insert">
	 insert into tbl_question(question_title, question_content, question_writer)
	 values(#{question_title}, #{question_content}, #{question_writer})
	 </insert>
	 
	 <insert id="delete">
	 delete from tbl_question where question_bno=#{question_bno}
	 </insert>
	 
	 <insert id="update">
	 update tbl_question set question_title=#{question_title}, question_content=#{question_content}
	 where question_bno=#{question_bno}
	 </insert>
	 
</mapper>